# Epitar

**Epitar** is a custom implementation of GNU tar utility. 
It is used to archive multiple files into a tarball and extract them, reproducing the core behavior of the original Unix command.

## Building the Project
To compile Epitar, you will need `gcc` and `make`. Simply clone the repository and run `make` at the root of the project
